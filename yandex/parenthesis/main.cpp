#include <vector>
#include <iostream>
#include <string>

std::string genParenthesisSeq(int n, char prev) {
    if (prev == '(')
        return gen')'
}

int main(int argc, char* argv[]) {
    int n;
    std::cin >> n;

    std::vector<std::string> v;

    for ()

    return 0;
}